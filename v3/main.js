import hljs from 'highlight.js';
import 'highlight.js/styles/github.css';

window.addEventListener('DOMContentLoaded', () => {
    //NOTE: the best options are color-brewer & ascetic
    hljs.highlightAll();
});